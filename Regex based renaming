Regex based renaming 

We can you use the `rename` command to rename many files at once according to a pattern. 

For example to move all .bak files back to original we do:
```
rename 's/\.bak$//' *.bak
```

