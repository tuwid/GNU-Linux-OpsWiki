Extract files using 'tar'

To extract an archive to a directory different from the current, use the `-C` flag , or its equivalent `--directory`, tar option, as in
```
tar -xf archive.tar -C /target/directory
```

For more information on the tar command check its man page: 
```
man tar
``` 
